{
  "buildCommand": "npm --prefix foundit-frontend install && npm --prefix foundit-frontend run build",
  "outputDirectory": "foundit-frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
